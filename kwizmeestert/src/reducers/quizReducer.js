import {} from '../constants';

const initialState = [];

const quizReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default quizReducer;
