{
  "name": "server",
  "author": "Albert Veldman",
  "version": "0.1.0",
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd kwizmeestert/ && npm install && npm run build && cd ../team/ && npm install && npm run build && cd ../scorebord/ && npm install && npm run build"
  },
  "dependencies": {
    "body-parser": "^1.17.1",
    "express": "^4.15.2",
    "mongodb": "^2.2.25",
    "ws": "^2.2.2"
  },
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "cacheDirectories": [
    "node_modules",
    "kwizmeestert",
    "team",
    "scorebord"
  ],
  "engines": {
    "node": "7.8.0"
  }
}
