{
  "name": "server",
  "author": "Albert Veldman",
  "version": "0.1.0",
  "scripts": {
    "start": "node server.js",
    "build": "cd kwizmeestert && npm run build && cd ../team && npm run build && cd ../scorebord && npm run build"
  },
  "dependencies": {
    "body-parser": "^1.17.1",
    "express": "^4.15.2",
    "mongodb": "^2.2.25",
    "ws": "^2.2.2"
  }
}
